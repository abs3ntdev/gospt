kind: pipeline
type: docker
name: default

steps:
- name: build
  image: golang
  commands:
  - go build
- name: gitea_release
  image: plugins/gitea-release
  settings:
    api_key:
      from_secret:
        GiteaKey
    base_url: https://gitea.asdf.cafe
    files: gospt

trigger:
  event:
    - push
    - tag
  when:
      event:
      - tag
      ref:
      - refs/tags/v*
